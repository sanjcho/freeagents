%table.table.table-hover
  %thead
    %tr.danger
      %th= players.first.role.pluralize
    %tr
      %th Name
      %th Team
      %th Logo
      %th Cap Hit
  %tbody
    - players.each do |player|
      %tr
        %th= player.name
        %th= player.team.name
        %th 
          %img{src: player.team.image}
        %th= player.caphit

